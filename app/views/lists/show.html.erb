<h1><%= @list.name %></h1>
<% @list.bookmarks.each do |bookmark| %>
  <%= bookmark.comment %><br>
  <%= image_tag bookmark.movie.poster_url, width: 200 %><br>
  <%= bookmark.movie.rating %>
<% end %><br>

<%= link_to 'Go back', lists_path %>
<%= link_to 'Delete Bookmark', bookmark_path%>
